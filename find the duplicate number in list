class Solution:
    def findDuplicate(self, nums: List[int]):
        # for ele in nums:
        #     if nums.count(ele)>1:
        #         return ele
        
        nums.sort()
        n = len(nums)-1
        for i in range(n):
            if nums[i] == nums[i+1]:
                return nums[i]
